export enum CallType {
  Veteran,
  Guardian,
  MailCall
}

export enum CallStatus {
  New,
  NoAnswer,
  LeftMessage,
  WaitToCallBack,
  PhoneDisconnected,
  WrongNumber,
  Accepted,
  Tentative,
  Declined,
  Future,
  FlownAlready,
  Remove,
  Deceased
}

